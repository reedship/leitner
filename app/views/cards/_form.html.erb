<%= form_with(model: card) do |form| %>
  <% if card.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(card.errors.count, "error") %> prohibited this card from being saved:</h2>

      <ul>
        <% card.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :front, style: "display: block" %>
    <%= form.text_field :front %>
  </div>

  <div>
    <%= form.label :back, style: "display: block" %>
    <%= form.text_field :back %>
  </div>

  <div>
    <%= form.label :deck_id, style: "display: block" %>
    <%= form.text_field :deck_id %>
  </div>

  <div>
    <%= form.label :interval, style: "display: block" %>
    <%= form.text_field :interval %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
